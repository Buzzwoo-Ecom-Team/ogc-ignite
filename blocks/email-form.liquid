{% render 'standard-blocks', type: 'email_form' %}

{% schema %}
{
  "name": "Email signup form",
  "tag": null,
  "settings": [
    
  {
    "type": "range",
    "id": "signup_max_width",
    "min": 200,
    "max": 600,
    "step": 10,
    "default": 340,
    "unit": "px",
    "label": "Maximum width"
  },
  {
    "type": "inline_richtext",
    "id": "signup_success_message",
    "label": "Sign up success message",
    "info": "Leave empty to use the standard theme translation."
  },
  {
  "type": "checkbox",
  "id": "use_headline_bg",
  "label": "Use custom background for heading (defined in Color Scheme)",
  "info": "pimped with ♥ by Buzzwoo! (if you want to override it in custom css, use class '.headline-with-bg' with properties 'background' & 'color')",
  "default": false
}

  ],
  "presets": [
    {
      "name": "Email signup form",
      "category": "Specialized"
    }
  ]
}
{% endschema %}
